import { type EventWithDetail } from './shared';

// window.addEventListener("phx:scroll-into-view", ev => {
//   let event = ev as EventWithDetail;

// 	setTimeout(() => {
//     document.getElementById(event.detail.id)?.scrollIntoView(true);
// 	}, 200);
// });

